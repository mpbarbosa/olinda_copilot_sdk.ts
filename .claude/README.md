# `.claude/`

This directory stores local Claude Code metadata for this repository checkout.

- `settings.local.json` defines tool permission allowances used when Claude Code
  operates in this repository.
- The contents are developer-environment configuration, not runtime library code.
- Keep the directory in version control so collaborators inherit the same local
  guardrails when working on the project.
