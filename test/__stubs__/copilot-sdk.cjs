'use strict';
// Minimal CJS stub for @github/copilot-sdk (pure-ESM package).
// Each test that needs this module provides a full jest.mock() factory.
// This file exists solely so Jest's CJS resolver can locate the module name.
class CopilotClient {}
function approveAll() {}
module.exports = { CopilotClient, approveAll };
