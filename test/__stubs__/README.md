# `test/__stubs__`

Shared test stubs and lightweight CJS shims used across the test suite.

| File | Purpose |
|------|---------|
| `copilot-sdk.cjs` | CommonJS stub for `@github/copilot-sdk` — used by integration tests to verify CJS build output without a live SDK dependency |
