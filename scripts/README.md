# `scripts`

Utility and automation scripts for local development and deployment.

| File | Purpose |
|------|---------|
| `deploy.sh` | Publishes the package — runs build, validates output, and pushes to the registry |
